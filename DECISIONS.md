# 自行确定事项与数据处理说明

本文记录需求未明确、由实现阶段补全的产品、交互、数据与本地化决策。

## 1. 名称和产品结构

- 产品名称统一为 `TL2 Wiki`，页面标题、导航、首页和页脚不再使用其他品牌名称。
- 一级页面为首页、角色、机制、装备、技能书和相位兽。
- 信息检索优先于展示：装备使用分页表格，技能书按技能族合并等级，职业技能使用树内列表和固定详情栏。
- GitHub Pages 使用 Hash 路由与相对资源路径，仓库名称改变时无需修改 Vite `base`。

## 2. 数据来源和转换

基础记录采用公开的 [Awkward-im/Torchlight](https://github.com/Awkward-im/Torchlight) 项目所提供的 `tl2db_base.db.7z`。该 SQLite 数据库由游戏基础数据整理而来，含 6,262 条物品行和 1,659 条技能行。装备数值和词条再与 Deth Guild 保存的 [TIDBI-eng v1.05](http://www.dethguild.com/torchlight_item_database.php) 交叉关联；该 Access 数据库含 6,050 条物品、7,646 条效果和 365 条套装奖励定义。

`scripts/refresh-tl2-data.sh` 负责下载、解压两套数据库并调用转换器；`scripts/import-tl2-db.mjs` 完成以下规范化：

1. 使用 SQLite 展示 ID 的十进制字符串，避免 JavaScript 读取 64 位整数时丢失精度。
2. 仅保留具有展示名称的玩家装备类型、镶嵌物和技能书。
3. 排除任务道具、药水、地图、鱼、金币、无标题模板以及 `NOSPAWN / NO_DROP / TEST` 内部记录。
4. 将游戏 `UNITTYPE` 统一映射为 `weapon / armor / trinket / pet / socketable`；项圈和饰钉单列为宠物装备，不混入角色饰品。
5. 将 `NORMAL / MAGIC / UNIQUE / LEGENDARY` 及 `itemset` 映射为面向玩家的 `普通 / 稀有 / 独特 / 传奇 / 套装`。
6. 清除游戏颜色控制码、异常空白和换行，保留内部名、图标键及 DAT 来源文件供追溯。
7. 使用内部 `ConsolNAME` 跨库关联；4,366 条可用装备中有 4,364 条直接匹配，而不是用易重名的展示名称猜测。
8. 以 TIDBI 补齐精确的五元素伤害/护甲范围、实际属性需求、装备等级、需求等级、孔数、速度、格挡、职业限制、物品效果和套装奖励。
9. 将属性需求映射为 `STR / DEX / FOC / VIT`，保留原数据库路径供追溯；64 位 SQLite ID 先格式化为十进制字符串，避免 JavaScript 精度丢失。
10. 依据游戏的 4 职业 × 3 技能树 × 每树 10 技能结构，从技能表选出完整的 120 个玩家技能；排除同名召唤子技能、旧版技能和怪物技能。
11. 技能书保留 194 个实际等级记录，并从标题中规范化技能族和罗马数字等级。

最终数据量为 4,366 条装备、7,147 条已关联物品效果、365 条套装奖励定义、194 条技能书及 120 个职业技能。数量、匹配覆盖率和生成日期同时写入 `public/data/meta.json`，页面直接展示该元数据。

## 3. 字段限制和内容补充

- TIDBI 的 `effects` 表按内部名称关联到装备，站点直接展示其英文效果文本及数值；不再以手写样例充当装备词条。
- 两套源数据库都没有统一的掉落实体字段。已确认的特殊挑战掉落通过转换器中的补充映射写入 `specialSource`；所有 `The Eye of …` 物品按明确命名规则关联到对应首领。其余记录明确标为“未标记特殊掉落”，不猜测地点。
- 两条无法与 TIDBI 对齐的可用装备仍保留 SQLite 的真实基础字段，并以 `tidbiMatched: false` 标记；这不是空白模板，也不伪造精确范围或效果。
- 同名且同类型的多个数据库记录在详情中作为可切换版本展示，避免擅自将其解释成具体 NG 周目；数据库本身未提供可靠的 NG 标签。
- 游戏原始英文文本优先。未在合法来源中取得官方简中字符串时，不批量机器翻译数千个物品名，也不把编辑性翻译标成官方文本。

## 4. 搜索和多语言

- `Ctrl/Command + K` 和非输入状态下的 `/` 均可打开全局搜索；`Esc` 关闭。
- 搜索覆盖职业、120 个职业技能、全部规范化装备、194 条技能书和相位兽区域。
- 语言控件使用显式下拉框，显示当前语言全名，不再以“中文 / EN”按钮暗示目标语言。
- 语言选项由 `localeOptions` 注册表驱动；添加语言需要注册代码、补齐 UI 字典并决定内容字段的回退策略。

## 5. 视觉与排版

- 继续使用暖米色、炭黑和陶土橙的 Anthropic 式配色，但去除大面积徽记、装饰轨道和无法由当前字体显示的符文字符。
- 主页只保留名称、用途、搜索和数据入口；减少口号、隐喻和大面积空白。
- 页面头部、卡片、技能栏和详情栏采用统一边框、紧凑字号与明确列结构。
- 四属性颜色固定为 STR 红、DEX 绿、FOC 蓝、VIT 黄；仅在需要建立属性关联时使用。
- 相位兽图片使用内置图像生成工具制作一张无文字的本地 WebP 插图，六个区域通过不同裁切位置复用；避免外链失效、授权来源混乱和字体豆腐块。

## 6. 机制与区域依据

- [Torchlight II 官方职业页](https://www.torchlight2game.com/classes)：职业英文定位。
- [Torchlight Wiki — Stats (T2)](https://torchlight.fandom.com/wiki/Stats_%28T2%29)：属性效果和社区测试公式。
- [Torchlight Wiki — Class Skills](https://torchlight.fandom.com/wiki/Engineer_Skills)：四职业完整技能树次序的交叉核对。
- [Torchlight Wiki — Phase Beast](https://torchlight.fandom.com/wiki/Phase_Beast)：六个区域、出现规则和随机挑战池。
- [Arc Games 语言支持说明](https://support.arcgames.com/hc/en-us/articles/360017559914-Language-support-for-Torchlight-II)：官方简体中文支持版本说明。

机制数据可能随平台版本或模组改变。站点按基础游戏数据处理，模组内容不混入基础库。
