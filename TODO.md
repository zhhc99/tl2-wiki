# TODO

## 修改基于 e401eb2

- 不要在任何地方明确指定具体字体. 应该使用通用语义, 如 serif, system ui 这类.
- 面向用户而不是开发者交互: 去除所有面向开发者交互的痕迹, 例如 "更快找到职业、装备、技能书和游戏机制。数据来自游戏数据库，并保留原始英文名称。"查看数据说明 -> decisions.md", "按技能族合并展示全部等级", "数据源：TL2 game data + TIDBI v1.05" 以及其他所有同理表述. 不要在产品里说你做了什么, 如 "属性公式、收益曲线和触发规则集中在一页。".
- 角色按照战猎法工的顺序呈现. 不要给角色擅自推荐属性或加标签.
- 禁止自行翻译任何游戏文本. 你应该寻找官方可信来源的中文翻译对照. 这包括技能名, 技能树名, 等等. 对于缺失的内容, 回退到英文.
- 技能描述应该和原始游戏一致.
- 属性效果应该是 **人类可读** 的文本. 反例: "缩放固定技能伤害，包括物理伤害", "处决加成使用相同的递减公式", 等等. 建议完全重写, 因为除了力量的文本外, 其余文本基本全部不可读. 删除属性计算器. 以人类可读, 话语自然为目标改进 "命中与触发矩阵", 并移除 "不要只看技能动画；技能模板与具体伤害段才决定能否触发。" 以及所有同类 ai 味文本.
- 字体尺寸和 ui 设计应该修改, 以满足可读性规范, 参考 material design.
- 不要强调 "非官方资料站". 删去所有相关文本.
- 相位兽: 目前内容完全不可读, 对玩家没有任何帮助. 建议先自己弄清相位兽的机制, 然后自行设计合适的页面组织形式和呈现信息.

## 修改基于 4a53b92

现在有了新的中英文 (中文有简繁) 数据库 `./tl2-wiki-data`, 包含文本和图片. 完善多语言和图标等.

- 技能文本应该尽可能和游戏内一致. 目前的缺乏具体数据信息.
- 属性机制文本应该善用颜色, 粗体等信息来突出重点或词条. dex 的信息可以以这个思路写: 第一条是 "每点敏捷提高约 xx 暴击几率和 xx 闪避几率", 第二条解释具体衰减公式和上限. vit 的格挡率部分同理.
- 工作后, 列出仍然缺失的数据.
- 不再维护 decisions.md. 它已经被移除.
- 可能需要更新 gitignore.

## 修改基于 bb98ae4

细节修改:

- 套装应该和稀有度是互不冲突的词条, 而不是替换稀有度.
- 根据游戏中稀有度的颜色, 对应调整物品的 tag 颜色和图标边框颜色.
- 所有下拉选择框的样式都缺失, 导致外观非常老旧. 应该补充. 另外, 装备列表页面下拉框的布局似乎和语言选择框不一致, 语言框布局更好.
- 统一属性机制中关于 "%" 的表述: str 和 foc 不要修改; dex 和 vit 向 str/foc 的表述形式靠. 将 "能否暴击、吸取或触发武器效果，取决于伤害来自哪里。普通攻击、使用武器伤害的技能、固定技能伤害、持续伤害和召唤物遵循不同规则。" 替换成其他更短, 更像人类的表述.

## 修改基于 d131b52

装备改进:

- 需求等级, 掉落范围 (的上限), 装备效果多语言普遍缺失, 数据库已经尽可能补充.
- 不需要白装, 它们对配装没有价值.
- 所谓速度值应该只对武器展示. 它的含义看起来是 "基础攻击间隔".
- 关于孔数: 任何装备都可在打孔师处提升至最多 2 孔. The Asphyx 套装的 2 件为 5 孔. 任何蓝色装备在掉落时可以有至多 4 孔的变种. Netherrealm 系列的双手武器为 5 孔, 单手武器为 4 孔. 将孔数信息从装备面板中移除, 并将该机制 double check 后将实际机制写在机制面板内.

技能书:

- 数据库已经补充技能书的更多多语言数据. 更新多语言.

配装:

- 添加配装面板, 位于装备之后.
- 让玩家模拟选择自己身上的一套装备 && 加点, 然后检视属性.
- diablo / diablo-like 的配装 UI 实现已经很成熟, 参考已有 wiki, 做出符合项目气质的现代化可读 UI.

赌博:

- 添加赌博面板, 位于配装之后.
- 赌博面板讲解赌博机制, 并提供赌博计算器: 根据物品类型, 等级, 孔数, 可以计算 8 个可能价格 (实际为 4 个, 但因舍入关系为 8 个). 参见 http://home.ustc.edu.cn/~ustczhc/projects/project-torchlight/tl.html 的底层逻辑, 但应该有全新设计的现代化可读 UI (参考 material design).

数据库: 这部分内容在完成上述所有任务后告知我.

- 确认当前项目所需的数据库结构, 以及全部所在位置. 确认这些位置是否被 ignore.
- 确认当前项目是否对外部数据库有依赖.
- 确认当前仓库的尺寸, 以及提交后该如何部署到 github page.

## 修改基于 16e84db

UI & 功能改进: 注意符合 material design 的方向和原则.

- 下拉框仍然缺少现代化样式.
- 主页: `lucide lucide-arrow-right` 事实上成为单独一行, 不符合设计美学; 四个 `stat-pill` 文字位置偏移, 显示效果差.
- 配装: 设计交互, 使得允许速览装备效果.
- 装备: 伤害, 护甲等基础属性应该显示在职业需求之下, 物品效果之上, 而不是最下方.
- 机制: 孔数规则用一段有条理的文字表示. 大致顺序: 一般初始孔数, 打孔上限, 特殊装备. 注意本地化.

语言改进:

- 装备: "基础攻击间隔" -> "攻击速度". 其他语言做对应修正, 下同.
- 对于非游戏内置的翻译文本, 繁体中文应采用正体中文 (台湾) 的表达习惯, 而不是简中 + 替换字.
- 赌博: "怎么算出来" 机器翻译痕迹严重. 做调整.

数据库:

- 数据库已更新到 schema v5, 尝试利用新补充的数据改进装备效果的显示问题.

## 修改基于 b3fe8f4

技能, 装备和套装描述改进. 目的如下:

- 修复技能, 装备, 套装的描述问题.
- 解决固伤类或一些其他技能的数值随角色等级而 scale, 从而难以描述效果的问题.

配装页面改进:

- 装备栏搜索 / 切换角色时, 应该自动过滤 / 卸下职业不符的道具. 对于属性和等级需求都不符合的装备, 在装备栏用符合 material design 语义中表示 "不满足要求" 一类的视觉装饰来提示用户.
- 装备栏的预览: 选择装备时应该呼出预览, 然后通过预览添加到装备栏, 而不是直接添加到装备栏. 预览界面的 UI 会将需求属性显示得好像 "获得属性" 一样, 应该将其移动至需求等级处.
- 属性检视面板有价值, 但存在误导: 用户可能以为是全身装备的属性, 实际是基础属性点获得的那部分属性. 在做完装备和套装描述改进后, 该面板应该能显示玩家当前配装下的完整属性 (而不只是属性点属性). 应该依次显示如下内容:
  - 武器基础伤害, 武器伤害加成; 基础护甲, 护甲加成; 暴击率, 暴击伤害加成; 额外生命, 闪避率; 额外法力, 元素伤害加成; 格挡率, 处决率; 全伤害增加 (对应 "+X%的全部伤害" 类效果), 全伤害减免 (对应 "遭受的全部伤害降低" 类效果. 某些职业可能自带这一数值, 需确认);

## 修改基于 678dd84

UI, 交互与细节修改:

- 配装应该使用浏览器缓存, 确保用户在切换导航或刷新后仍从旧结果开始.
- 装备前预览时, 点击装备到栏位后, 应该关闭弹出的卡片, 而不是将卡片切换为速览.
- 可读性问题: `slot-unmet` 和上方装备等级元素重叠.
- 装备栏顺序改为: 主手, 副手; (各种甲)... 项链; 戒指 1, 戒指 2. 注意处理双手武器的逻辑.
- 装备页面的装备描述版面简化示例:
  - `detail-grid` 现在已经不需要.
  - 在 `subtype` 处使用 "Lv.105 腰带" 的表述. 原先为 "腰带". 对应新语义更新 class 标签名.
  - 现在装备需求栏提供全部需求信息. 一般装备的需求形如 "Lv.xxx\n  或\n属性A yyy && 属性B zzz\n职业: uuu", 按需缩减, 按可读性规范使用加粗等修饰. 仍然在此处用 `stat-pill` 表示属性. 新的装备需求栏位于其他数值栏上方, 而不是版面最上方.
  - 对于武器, `detail-grid` 还有攻击速度, 将其移动到 "基础伤害范围" 区域. 该区域不再需要小标题, 直接用游戏内风格语义排版显示 DPS (直接在前端计算, 舍入到整数, 不考虑计算和游戏内舍入次序导致的细微误差), 攻击速度和所有伤害组成. 例如 (Blastmaw's Trigger, 数值仅为示例):
  ```
  **917 Damage per Second**
  **0.56s Attack Speed**
  Physical Damage: xxx-yyy
  Fire Damage: xxx-yyy
  Poison Damage: xxx-yyy
  ```
  - 对于带护甲的部位的装备, 用类似的游戏内风格语义排版显示原基础护甲范围面板的信息, 同样不需要小标题. 如
  ```
  114 [color]Ice Armor[/color]
  114 [color]Fire Armor[/color]
  114 [color]Electric Armor[/color]
  114 [color]Poison Armor[/color]
  ```
- 赌博的可能价格卡片可读性问题: 该卡片 h2 与下方 `price-table` 的间距过小. 这可能预示着更深的问题, 不应通过硬编码这一处间距来解决.

装备数据修改:

- 修复装备数据不准确的问题. 这包括未舍入, 舍入误差, 未考虑一些影响数值的参数等.

## 修改基于 fcda043

可读性修改:

- 配装界面的武器速览 (含装备前预览, 下同) 还应该能看到攻击速度和套装效果. 考虑到套装效果可能很长, 应该根据 material design 规范, 用一个默认收起的手风琴等价物展示.
- 装备界面的 `data-table` 不再显示需求等级, 该列版面用来显示职业限制 (表头: 职业; 若装备无职业, 则为空).

功能改进:

- 配装界面可以给道具填充宝石. 添加一个只读面板, 可看到当前塞入的全部宝石 (宝石根据镶嵌位置有两个效果, 此面板只显示宝石被触发的那个效果). 宝石效果加入装备效果汇总.

## 修改基于 4f6470b

配装功能改进:

- 镶嵌宝石时, 只显示能镶嵌的那些. 在选择宝石的界面也显示宝石的等级.
- 在装备栏界面, 也用最简洁且符合可读性设计规范的元素显示每件装备的宝石孔镶嵌情况.

装备数据改进:

- 对于多 NG 中数值变化的物品, 显示所有 NG 的变种 (在名称后面用 "(NG+)" 这类标签来区分; NG 的变种不带标签). 把每个变种当成单独的物品处理.

## 修改基于 6597cde

配装功能改进:

- 将低于 2 孔的橙, 红装视作 2 孔. 将任何蓝装视作 4 孔.
- 在 `build-effects` 面板中, 对于容易堆叠 (如, 非触发) 的词条, 将其数值累加后展示. 例如, 不再显示 "+20点敏捷 × 2", 而是直接显示 "+40点敏捷". 改进完成后, 将实现的词条在输出的最后列举, 并说明数据侧是否对该功能友好.
- 移除 `build-status` 面板.

图标改进:

- 将 `agent/icon.ico` 在恰当位置创建副本, 作为站点图标.

## 修改基于 3a3f300

配装改进:

- 允许 (通过剪贴板文字, 权限被阻止时有回退措施) 导入导出配装. 界面和交互规范遵循 material design.

## 修改基于 47ed50e

机制修正:

- 普通非武器蓝装的最大孔数为 2 而不是 4. 确认相关机制 (注意确认盾牌), 然后修复配装和机制页面的错误逻辑 / 文本.

## 修改基于 77dc523

技能数据修正:

- 修复元素大师等技能的数据错误. 这可能是应用了错误的缩放或错误的图表所致.

## 修改基于 3e4f1de

功能改进:

- 配装页面的搜索功能应该能搜索名称, 套装或效果.
- 装备页面查看物品信息时, 如果不是宠物用品或镶嵌物, 则可以快速导航到对应赌博页面. 使用装备在数据库中的原有孔数.

## 修改基于 adbc873

问题修复:

- 以下 spell 在游戏内无法获得, 仅存在数据. 将从数据库中提取的 spell 数据添加标签, 然后前端通过识别该标签来过滤 (直接不显示这些 spell).
  - identify spell; waypoint portal spell; poison cloud; summon blood skeleton; summon aloe gel; summon flaming sword; critical strikes;
  - 确认以下技能是否可获取: wirling flames, tunnelers, web, summon blood zombie, bee swarm. 将那些确认无法在游戏内掉落 (不含控制台) 的也打上上述标签.

## 修改基于 f9d8a7f

问题修复:

- 使用顶部全局来查找时, 右侧的文本 (如 "ITEM") 未使用多语言. 当搜索到装备时, 点击搜索结果也不会执行对该装备的搜索, 而只是导航到搜索页面.
- 在装备库的其他数值添加 2 个元数据, 位于 "掉落等级" 的下方:
  - SOCKETS
  - RARITY
- 所有数字输入框都不便于使用: 假如默认值是 50, 用户想输入 22, 于是全选 - 删除, 然后数值变成了 1, 用户体验归零. 寻找现代化, 交互友好, 逻辑简单的实现.
- 职业技能的蓝耗在前端和角色等级也相关, 在游戏内则只和技能等级相关. 确认是否存在逻辑问题, 如果存在则修复.

## 修改基于 bf7d8ce

数据库更新了一些缺失的掉落范围上界, 同时数据库路径已经从 `tl2-wiki-data/` 迁移到符号链接 `database/`. 重新执行数据导入.

其他问题修复:

- 非 Lv105 的 Legendary 不能通过赌博获得. 用维护性良好, 心智负担最低的方法让其在装备信息面板中的 "计算赌博价格" 变成 "无法赌博", secondary text 从 "带入类型...孔数" 变成 "只能从掉落中获得". 对应该文本做多语言.
- 装备信息面板中 "其他数值 - 格挡几率" 应该列入各种防御力所在的那一栏, 作为最后一条数值显示.

## 修改基于 430b64a

现在弄清了准确的赌博公式:

P = trunc[(1 + 0.1S) × ceil(V × (400 + 100[L + max(0, randInt(-3, 3))]) × 0.01_f)]

其中 P 为赌博价格, L 为装备的等级 iLvl, V 为装备的 VALUE 值 (一般在当前算法下按等级获取, 有少数例外), 缺省为 100. 另外, P 必须 >=1.

数据库已有和 VALUE 相关的一个视图 (`v_item_values`). 一般情况下, VALUE 仍然和当前数值相同, 只对少数物品有例外.

- 将这个公式作为赌博页价格计算方式 "得到四档价格" 的 tooltip.
- 价格计算方式 "物品类型决定基础系数" 添加 tooltip: "部分 RARITY 较低的物品除外."
- 价格计算器中, 删除计算式卡片, 并添加一个 "具体物品" 的装备选择器, 样式类似装备栏的一项, 点击可以搜索并选择对应类型的一件装备 (同样类似装备栏的处理). 如果选择了物品, 则用该物品的 VALUE 值替代. 可能价格中, 根据当前准确算法, 现在应该只剩 4 个价格, 另外也不需要下方 "共有 4 个原始价格..." 的 secondary text. 赌博页面标题的副标题中 "八个整数价格" 也对应更新. 所有文本更新都要同步到多语言.
- 现在从装备的物品信息导航到赌博时, 直接将具体物品选择器置为该物品.
- 装备的物品信息最下方 "其他数值" 部分在 `RARITY` 之后显示 `VALUE` 值.

可能需要对应改进架构. 先做评估再进行实现, 确保架构简单, 维护负担轻.

## 修改基于 e9544e9

架构已经完全重构, 现在已经基于面向本项目的数据库工作.

功能改进:

- 不再给技能书显示 school 标签. 不再维护该内容, 也不保留任何历史包袱和该标签存在的痕迹.
- 职业文本, 技能树文本之前需要手动重建, 现在数据库已经更新, 一些视图也改变:

```
  - 新增职业、职业翻译、技能树、技能树翻译规范化表。
  - class_skill_layout 改为通过 tree_id 关联技能树。
  - 新增 wiki_classes、wiki_class_skill_trees 视图。
  - wiki_class_skills 增加 tree_id。
  - 从翻译表和 SKILL_TREE_*.LAYOUT 自动提取三语文本及顺序。
  - Schema 版本升级至 3。
```

基于数据库新架构完成跟进.

文本改进 (方括号为 tooltips; 适当使用加粗强调; 所有文本修改都要应用多语言):

- 力量文本修改:
  ```
  每点还增加 0.4% 暴击伤害；暴击默认造成普通伤害的 150%。 -> 每点增加 0.4% 暴击伤害[初始暴击伤害为 +50%]。
  ```
- 专注文本修改:
  ```
  每点专注增加 0.5% 的任何固定伤害[例如塞外客「投掷回旋镖」的伤害、武器的物理 DoT 伤害。]或元素伤害[包括元素化的武器伤害和 DPS% 伤害。]。 -> 每点专注增加 0.5% 的元素伤害[不在属性面板的元素伤害上体现。]和 0.5% 的物理固伤[例如武器的物理 DoT 伤害、塞外客「破碎之刃」的伤害]。
  处决几率使用同样的递减曲线：专注 ×（0.2002 − 0.0002 × 专注）%；专注最多贡献 50%。 -> 每点专注增加 0.2% 的处决（猛击）几率[准确加成为 专注 ×（0.2002 − 0.0002 × 专注）%，最多贡献 50%。]。
  ```
- 敏捷文本修改:
  ```
  两项几率各自的准确加成为 敏捷 ×（0.2002 − 0.0002 × 敏捷）%。投入越多，每点收益越低；敏捷对每项几率最多贡献 50%。-> 投入越多，每点收益越低[两项几率各自的准确加成为 敏捷 ×（0.2002 − 0.0002 × 敏捷）%，最多贡献 50%。]。
  ```
- 体力文本修改:
  ```
  装备盾牌时，每点体力起初约增加 0.2% 格挡几率。准确加成为 体力 ×（0.2002 − 0.0002 × 体力）%；投入越多，每点收益越低，体力最多贡献 50% 格挡几率。 -> 每点体力增加 0.2% 的格挡几率[准确加成为 体力 ×（0.2002 − 0.0002 × 体力）%，最多贡献 50%。]。
  ```
- 删除孔数规则板块.

修复:

- 全局搜索 NG 变种装备时, 不再将 "NG+" 这类文本加入搜索 (例如, 点击 "纳格斯雷的指环 NG+", 错误的搜索: "纳格斯雷的指环 NG+"; 正确的搜索: "纳格斯雷的指环").

## 修改基于 439904a

多语言改进:

- 根据 `docs/non-database-text-fixed.csv` 执行多文本替换.

## 修改基于 f75d19b

使用 RR7 添加 SEO 支持. 数据库对应更新 `family_id` 字段用于 slug.

SEO 的实现方式见 `docs/seo.md`. 进行了 3 轮 质量评估 -> 跟进 的循环. 质量评估包括 SEO 规范性, 代码简洁性, 以及构建产物质量和尺寸.

## 修改基于 0309662

配装功能改进:

- 底部镶嵌物面板合并相同 (同一镶嵌物且应用同一效果的) 镶嵌物.

行为修复:

- 进入主站时可能的 locale 跳转未显示.
- 配装页面的 url 已经改变了, 不再是之前带 hashtag 的了, 所以导出配装时的文本需要修改. 注意现在还有多语言.

SEO:

- robots.txt 在 github pages 上没有有效方案 (因为 Google 只看根域). 直接删除.
- 实体页的 H1 仍是 "装备" "职业" 等栏目名, 而不是具体项目名. 用 seo.md 文档中的思路实现. 用测试确保 `items/arcgaps-vice/` 这类页面的 H1 是装备标题.

## 修改基于 f7d8da5

SEO:

- 明确 H1 规范并修正实现.

SPA 体验:

- 提高实体响应速度 (通过 RR Skip the Server Hop).

## 修改基于 a71da56

用户体验:

- 提高一级页响应速度 (扩展内存缓存), 添加加载提示.

## 修改基于 c9c6f2d

知识性修复:

- 赌博可行性判定修正: 现在类型合法的物品显示赌博卡片, 而 `RARITY>0` 的物品才可跳转.

## 修改基于 311fa94

知识性修复:

- 修复了一些技能数值错误的问题 (如塞外客 - 岩石契约).
